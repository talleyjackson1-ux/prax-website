/* FREE MOCK — Alex's performance marketing business (Lee's Summit, MO).
   Concept: THE ALIGNED LINE — "we only get paid when you do" drawn literally:
   the hero chart is two lines rising TOGETHER (client growth + our fee), with
   no fabricated axis numbers, because the incentive alignment IS the brand.
   Style per intake: classic & trusted — light theme, deep navy + slate,
   muted-gold accent, serif (Fraunces) headings, generous whitespace.
   Grounding: real phone 816-844-5548 only · "APA" wordmark = PLACEHOLDER until
   Alex's real logo lands · before/after gallery = labeled empty slots awaiting
   his real campaign screenshots · NO invented stats, reviews, or dollar figures. */
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './apa.css'
import Apa from './Apa'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <Apa />
  </StrictMode>,
)
